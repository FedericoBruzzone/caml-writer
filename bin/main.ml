(* open Camlwriter *)
(* open Caml_writer *)

let () =
    Camlwriter.main();
    (* let x = Caml_writer.Window_size.get_rows() in *)
    (* let y = Caml_writer.Window_size.get_columns() in *)
    (* match x, y with *)
    (* | Some rows, Some columns -> Printf.printf "rows: %d, columns: %d\n" rows columns *)
    (* | None, Some columns -> Printf.printf "Could not determine number of rows, columns: %d\n" columns *)
    (* | Some rows, None -> Printf.printf "rows: %d, Could not determine number of columns\n" rows *)
    (* | None, None -> Printf.printf "Could not determine number of rows, Could not determine number of columns\n" *)

